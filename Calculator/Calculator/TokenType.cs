namespace Calculator {
    public enum TokenType {
        Undefined,
        Start,
        Number,
        Lparen,
        Rparen,
        Multiply,
        Divide,
        Add,
        Subtract,
        End
    }
}